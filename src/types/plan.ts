// TODO: Define the PlanRef interface for the RiotPlan data model.
// This is a future stub — the shape will be defined when plan
// persistence is implemented.
export interface PlanRef {
  id: string;
  // TODO: Add plan-specific fields
}
