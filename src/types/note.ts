// TODO: Define the Note interface for the RiotPlan data model.
// This is a future stub — the shape will be defined when note
// persistence is implemented.
export interface Note {
  id: string;
  // TODO: Add note-specific fields
}
